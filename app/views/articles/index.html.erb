<h1>Cheap/Free Books</h1>

<% @articles.each do |article| %>
  <li><%= link_to article.title, article.url %> <%= link_to "Edit", edit_article_path(article) %> | <%= link_to "Delete", article_path(article), method: :delete, data: { confirm: "Are your sure?" } %></li>
<% end %>
